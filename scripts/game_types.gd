class_name GameTypes
extends RefCounted

enum Terrain {
	WATER,
	SAND,
	GRASS,
	STONE,
}

enum BuildingType {
	HUB,
	LOGGER_CAMP,
}

enum ResourceNodeType {
	TREE,
	STONE,
}

enum ResourceType {
	WOOD,
	STONE,
}
